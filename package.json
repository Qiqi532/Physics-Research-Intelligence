{
  "name": "physics-research-intelligence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "pnpm --filter @pri/db prisma:generate",
    "build": "pnpm --recursive --if-present build",
    "dev": "pnpm --parallel --filter @pri/web --filter @pri/worker dev",
    "lint": "pnpm --recursive --if-present lint",
    "pretest": "pnpm --filter @pri/db prisma:generate",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "pretypecheck": "pnpm --filter @pri/db prisma:generate",
    "typecheck": "pnpm --recursive --if-present typecheck"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
